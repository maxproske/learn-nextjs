import { roboto } from '@theme-ui/presets'

const theme = {
  ...roboto,
  colors: {
    ...roboto,
    background: 'grey',
  },
  containers: {
    page: {
      width: '100%',
    },
  },
  styles: {
    ...roboto.styles,
  },
}

export default theme
